# =============================================================================
# GLID Image Segmentation — direct dependencies
#
# Install:
#   pip install -r requirements.txt
#
# GPU (CUDA 12.8) builds are used here.  For CPU-only, remove the +cu128 tag:
#   torch==2.11.0
#   torchvision==0.26.0
# =============================================================================

# -----------------------------------------------------------------------------
# Core deep-learning framework
# Needed by: hive (all modules), project/analysis/*.py, notebooks
# -----------------------------------------------------------------------------
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# -----------------------------------------------------------------------------
# Numerical & image I/O
# Needed by: hive/data.py, hive/inference.py, project/analysis/helper.py
# -----------------------------------------------------------------------------
numpy==2.4.3
Pillow==12.1.1

# -----------------------------------------------------------------------------
# Data handling & progress
# Needed by: hive/train.py (pandas), hive/train.py + helper.py (tqdm)
# -----------------------------------------------------------------------------
pandas==3.0.2
tqdm==4.67.3

# -----------------------------------------------------------------------------
# Machine-learning utilities
# Needed by: project/analysis/helper.py, swin.py, unet.py (sklearn.metrics)
# -----------------------------------------------------------------------------
scikit-learn==1.8.0
scipy==1.17.1

# -----------------------------------------------------------------------------
# Visualisation
# Needed by: project/analysis/helper.py (matplotlib), notebooks (seaborn)
# -----------------------------------------------------------------------------
matplotlib==3.10.8
seaborn==0.13.2

# -----------------------------------------------------------------------------
# Notebook environment
# Needed to open and run project/codes/*.ipynb and project/analysis/*.ipynb
# -----------------------------------------------------------------------------
jupyterlab==4.5.6
ipykernel==7.2.0
ipython==9.12.0

# -----------------------------------------------------------------------------
# Display utilities
# Used in analysis notebooks (tabulate for pretty-printed tables)
# -----------------------------------------------------------------------------
tabulate==0.10.0

# -----------------------------------------------------------------------------
# Development & testing
# -----------------------------------------------------------------------------
black==26.5.1
pytest==9.1.1